Pflichtenheft – i18n Service (V1.0)
Service-Name: 	i18n-service
Version: 	1.0
Stand: 	16.02.2026
Geltungsbereich:	Microservice zur Verwaltung von Sprachen und Übersetzungs-	Bundles pro Projekt (Mandantenkontext via projectKey). Enthält 	Admin-Endpunkte zum Anlegen von Projekten/Sprachen, 	Upload/Versionierung von Bundles sowie einen Runtime-Endpunkt 	zur Abfrage von Übersetzungen (inkl. Default- und Fallback-Logik).

1. Einleitung
1.1 Ziel
Ziel ist die zentrale Bereitstellung von Übersetzungen für Frontends/Clients auf Basis von projectKey und languageCode. Der Service stellt eine konsistente REST-API (unter /api/v1/*) bereit, um Projekte und Sprachen zu verwalten, Sprachdateien (Bundles) hochzuladen und Übersetzungen zur Laufzeit effizient abzurufen.
Der Service stellt insbesondere sicher:
Projekt-Scope als Mandanten-/Kontextgrenze via projectKey
Sprachverwaltung (Anlegen, Lesen, Ändern, Soft-Delete, Restore, Purge)
Bundle-Management: Upload von JSON-Bundles, Download mit ETag, Versionierung, Rollback und Diff
Runtime-Translations: Abfrage kompletter Bundles oder Teilmengen (keys/prefix) mit Default-/Fallback-Logik
Audit-Log für relevante Änderungen (z. B. LANGUAGE_UPDATE, BUNDLE_UPLOAD)
Optionale Minimal-Absicherung von Admin-Endpunkten via X-Admin-Api-Key (ohne Spring Security im MVP)
1.2 Abgrenzung / Nicht-Ziele (MVP)
Nicht Bestandteil des MVP:
Vollwertige Authentifizierung/Autorisierung (JWT/OAuth/IAM) – aktuell nur optionaler API-Key-Filter
Unterstützung weiterer Bundle-Formate (z. B. YAML/Properties) – P0: nur JSON
Verschachtelte JSON-Strukturen (P0: flache Map key -> string)
Automatisches Synchronisieren/Build-Pipelines für Bundles (CI/CD) – wird extern erwartet
Mehrstufige FallBack-Ketten (P0: optional genau ein Fallback-Language-Code)
1.3 Begriffe
projectKey: Technischer Schlüssel (Regex ^[a-z][a-z0-9-]{2,31}$) zur Identifikation eines Projekts.
ProjectScope: Projekt-Konfiguration inkl. Default- und Fallback-Sprache.
languageCode: Sprachcode nach (vereinfachter) BCP-47-Logik, z. B. de, de-DE, zh-Hant-TW.
Bundle: Sprachdatei eines Projekts für eine Sprache. P0: JSON Map key -> string.
Bundle-Version: Historie der hochgeladenen Bundles je Sprache (1..n).
Default Language: Sprache, die genutzt wird, wenn die angefragte Sprache nicht verfügbar/aktiv ist.
Fallback Language: Optionale Zweitsprache, aus der fehlende Keys ergänzt werden können.
Soft-Delete: Logisches Löschen einer Sprache (Papierkorb). Restore möglich.
Purge: Endgültiges Löschen (Hard delete) einer Sprache inkl. Bundle-Referenzen/Dateien.

2. Rahmenbedingungen / Technologie
2.1 Technologie-Stack (MUSS)
Backend: Java 21 + Spring Boot
Build: Maven
Datenbank: MariaDB (Schema via Flyway)
API: REST/JSON unter /api/v1/*
OpenAPI/Swagger: springdoc (/swagger-ui.html, /v3/api-docs)
Zeit-Standard: UTC (DB + API-Zeitstempel als ISO-8601)
Bundle Storage: Dateisystem (konfigurierbar via app.bundle-storage.base-path bzw. APP_BUNDLE_STORAGE_BASE_PATH)
Cache: Caffeine (konfigurierbar: max size + TTL)
2.2 Deployment / Betrieb (SOLL)
Docker Image (Multi-Stage Build) + Docker Compose (MariaDB + Service)
Actuator Health Endpoint: /actuator/health
Konfiguration über Umgebungsvariablen (Port, DB, Storage-Pfad, API-Key)

3. Fachliche Anforderungen
3.1 Projektverwaltung (MUSS)
Projekt wird durch projectKey eindeutig identifiziert.
Projekt kann angelegt werden (POST /api/v1/projects).
Projekte können gelistet und einzeln geladen werden (GET /api/v1/projects, GET /api/v1/projects/{projectKey}).
Projekt-Konfiguration enthält defaultLanguageCode und optional fallbackLanguageCode (änderbar über Language-Settings-Endpunkt).
3.2 Sprache (Language) (MUSS)
Sprache ist eindeutig pro Projekt und languageCode (BCP-47-ähnliche Normalisierung).
CRUD: Create, Get, Update, List (active/all/deleted).
Soft-Delete (DELETE /languages/{code}) verschiebt in Papierkorb (is_deleted=true).
Restore (POST /languages/{code}/restore) stellt eine gelöschte Sprache wieder her.
Purge (DELETE /languages/{code}/purge?force=...) löscht endgültig; ohne force nur wenn bereits deleted.
Default- oder Fallback-Sprache darf nicht deaktiviert, gelöscht oder gepurged werden.
3.3 Spracheinstellungen (Default/Fallback) (MUSS)
Settings sind pro Projekt gespeichert (defaultLanguageCode, fallbackLanguageCode).
Update ist versioniert (Optimistic Lock): Request enthält expectedVersion.
Default muss existieren und aktiv sein; Fallback (falls gesetzt) muss existieren und aktiv sein.
GET/PUT Endpunkt: /api/v1/{projectKey}/language-settings.

3.4 Bundle Upload/Download (MUSS)
Bundle Upload als Multipart (POST /languages/{languageCode}/bundle, Feld file).
P0: nur .json Dateien, Inhalt muss eine flache JSON Map key -> string sein.
Key-Validierung: ^[A-Za-z0-9][A-Za-z0-9._-]{0,79}$.
Beim Upload wird eine neue Bundle-Version erstellt; das „Current Bundle“ zeigt immer auf die aktive Version.
Download des aktuellen Bundles (GET .../bundle) sowie einer konkreten Version (GET .../bundle/version/{n}).
Downloads liefern ETag (SHA-256) und unterstützen If-None-Match (304 Not Modified).
3.5 Bundle Versionierung, Rollback und Diff (SOLL)
Versionsliste (GET .../bundle/versions) liefert Versionen (neueste zuerst) inkl. Marker, welche Version aktuell ist.
Meta des aktuellen Bundles (GET .../bundle/meta) und Meta einer Version (GET .../bundle/version/{n}/meta).
Rollback (POST .../bundle/rollback/{n}) setzt das aktuelle Bundle auf eine frühere Version.
Diff (GET .../bundle/diff?from={a}&to={b}) liefert added/removed/changed Keys.
3.6 Runtime: Übersetzungen abfragen (MUSS)
Endpoint: GET /api/v1/{projectKey}/translations/{languageCode}.
Ohne Parameter: komplettes Bundle (sortiert nach Key).
Mit keys=A,B,C: nur diese Keys (max. 500). Fehlende Keys werden als Key selbst zurückgegeben.
Mit prefix=HOME.: liefert alle Keys, die mit Prefix beginnen (primary + optional fallback).
Falls angefragte Sprache nicht existiert/aktiv ist: es wird die Default-Sprache genutzt (Warnung im Response).
Optionaler Fallback: fehlende Keys werden aus Fallback-Bundle ergänzt; wenn nicht verfügbar -> Warnung.

3.7 Audit-Log (SOLL)
Änderungen (Projekt, Settings, Sprachen, Bundles) schreiben Audit-Einträge.
Audit enthält Zeitstempel, actor, action, entityType, entityKey, detailsJson, requestId.
Liste abrufbar über GET /api/v1/{projectKey}/audit-logs?limit=... (1..500), optional filterbar nach entityType + entityKey.
3.8 Request-Kontext (SOLL)
Pro Request wird eine requestId genutzt (Header X-Request-Id oder generiert) und als Response Header zurückgegeben.
Optionaler actor über Header X-Actor (sonst system).
Diese Werte werden in Fehlerantworten und Audit verwendet.
3.9 Minimaler Admin-Schutz (KANN)
Wenn app.admin.api-key gesetzt ist, müssen Admin-Endpunkte den Header X-Admin-Api-Key enthalten.
Offen (ohne API-Key): Runtime-Translations sowie Bundle-Downloads (GET) und Swagger/Actuator.
Geschützt: Projektverwaltung, Language-Settings, Language CRUD, Audit-Log, Bundle Upload/Rollback/Delete etc.

4. Nicht-funktionale Anforderungen
4.1 Performance / Skalierung (SOLL)
Runtime-Reads sollen DB-Last reduzieren durch Bundle-Cache (Caffeine).
Cache ist an Bundle-SHA gebunden: bei neuem Upload wird automatisch neu geladen (Invalidation).
Konfiguration über app.bundle-cache.maximum-size und app.bundle-cache.ttl-seconds.
4.2 Robustheit (MUSS)
Fehlerantworten sind konsistent (ProblemDetail, application/problem+json).
Optimistic Locking für Updates (Language und Project Settings) via row_version + expectedVersion.
Bundledateien werden bei Fehlern best-effort aufgeräumt (Upload-Rollback / Delete).
4.3 Sicherheit (SOLL)
Ohne zentrales IAM im MVP nur minimaler API-Key-Filter möglich.
Keine Binärdaten in der DB: Bundles liegen im Filesystem, DB hält nur Referenzen/Hashes.
Uploads sind größenbegrenzt (spring.servlet.multipart.max-file-size / max-request-size).

5. Datenmodell (logisch)
5.1 Tabellen (MUSS)
i18n_project_scope (Projekt + Settings, project_key UNIQUE)
i18n_language (Sprachen je Projekt, UNIQUE(project_key, language_code, is_deleted))
i18n_language_bundle (aktuelles Bundle je Sprache, UNIQUE(language_id))
i18n_language_bundle_version (Versionshistorie je Sprache, UNIQUE(language_id, bundle_version))
i18n_audit_log (Audit-Einträge, Indizes auf project+time und entity)
5.2 Wichtige Constraints (MUSS)
Projekt darf nur einmal existieren (uq_project_scope_key).
Pro Projekt/LanguageCode darf es max. eine aktive (nicht gelöschte) Sprache geben.
Bundle-Referenzen sind FK-geschützt (ON DELETE RESTRICT), daher müssen Bundles vor Sprache-Purge gelöscht werden.

6. REST-API Spezifikation (Basis: /api/v1)
Projekte
POST /projects – Projekt anlegen
GET /projects – Projekte listen
GET /projects/{projectKey} – Projekt lesen
Language Settings
GET /{projectKey}/language-settings – Default/Fallback lesen
PUT /{projectKey}/language-settings – Default/Fallback setzen (mit expectedVersion)
Sprachen
POST /{projectKey}/languages – Sprache anlegen
GET /{projectKey}/languages?view=active|all|deleted – Sprachen listen
GET /{projectKey}/languages/{languageCode} – Sprache lesen
PUT /{projectKey}/languages/{languageCode} – Sprache ändern (mit expectedVersion)
DELETE /{projectKey}/languages/{languageCode} – Soft-Delete
POST /{projectKey}/languages/{languageCode}/restore – Restore (Papierkorb)
DELETE /{projectKey}/languages/{languageCode}/purge?force=false|true – Hard delete
Bundles
POST /{projectKey}/languages/{languageCode}/bundle – Upload (multipart file)
GET /{projectKey}/languages/{languageCode}/bundle – Download aktuelles Bundle (ETag)
GET /{projectKey}/languages/{languageCode}/bundle/version/{n} – Download Version (ETag)
GET /{projectKey}/languages/{languageCode}/bundle/meta – Meta aktuelles Bundle
GET /{projectKey}/languages/{languageCode}/bundle/version/{n}/meta – Meta Version
GET /{projectKey}/languages/{languageCode}/bundle/versions – Versionsliste
POST /{projectKey}/languages/{languageCode}/bundle/rollback/{n} – Rollback
GET /{projectKey}/languages/{languageCode}/bundle/diff?from=a&to=b – Diff
DELETE /{projectKey}/languages/{languageCode}/bundle – Bundle komplett löschen (inkl. Versionshistorie)
Runtime Translations
GET /{projectKey}/translations/{languageCode} – Bundle/Keys/Prefix abrufen (offen)
Audit
GET /{projectKey}/audit-logs?limit=100&entityType=...&entityKey=... – Auditliste
7. DTOs / Beispiel-Payloads
7.1 Projekt: Create Request
{ "projectKey": "portal", "displayName": "Portal" }
7.2 Sprache: Create Request
{ "name": "Deutsch", "languageCode": "de-DE" }
7.3 Sprache: Update Request (Optimistic Lock)
{ "name": "Deutsch (formal)", "active": true, "expectedVersion": 0 }
7.4 Language Settings: Update Request
{ "defaultLanguageCode": "de-DE", "fallbackLanguageCode": "en-GB", "expectedVersion": 0 }
7.5 Bundle: Beispiel JSON Datei
{ "HOME.TITLE": "Willkommen", "HOME.LOGIN": "Anmelden", "COMMON.CANCEL": "Abbrechen" }
7.6 Runtime Translations: Response (keys)
{
  "projectKey": "portal",
  "requestedLanguageCode": "de-DE",
  "resolvedLanguageCode": "de-DE",
  "fallbackLanguageCode": "en-GB",
  "values": { "HOME.LOGIN": "Anmelden", "COMMON.CANCEL": "Abbrechen" },
  "missingKeys": [],
  "warnings": []
}
8. Caching (SOLL)
8.1 Ziele
Reduktion von DB-Last und Latenz bei häufigen Runtime-Reads (/translations/*).
8.2 Was wird gecacht?
Pro projectKey und languageCode wird das Bundle als Map im Speicher gehalten.
Cache-Entry ist an sha256 gebunden (ändert sich das Bundle, wird neu geladen).

8.3 TTL / Grenzen
Standard: TTL 300 Sekunden (konfigurierbar).
Max Entries: 2000 (konfigurierbar).
8.4 Invalidation (MUSS, falls Cache aktiv)
Nach Bundle Upload, Rollback oder Delete wird der Cache für die betroffene Sprache invalidiert.
Bei Purge einer Sprache wird ebenfalls invalidiert.

9. Fehlerformat (SOLL)
9.1 Einheitliches ProblemDetail
Der Service nutzt Spring ProblemDetail als Fehlerformat (Content-Type: application/problem+json). Typische Felder:
status, title, detail
zusätzliche Properties: requestId, actor
Typische HTTP-Statuscodes:
400 Bad Request (Validation/IllegalArgumentException)
404 Not Found (z. B. Projekt/Sprache/Bundle nicht gefunden)
409 Conflict (Version conflict, Duplicate, Concurrent upload)
401 Unauthorized (wenn Admin API-Key aktiviert und fehlt/ungültig)

10. Pagination & Sort (SOLL)
10.1 Standard
Im MVP gibt es kein allgemeines Paging für Listen-Endpunkte.
/audit-logs bietet limit (1..500) und sortiert absteigend nach occurredAt.
Sprachen/Projekte werden serverseitig stabil sortiert (z. B. nach languageCode).


11. Abnahmekriterien (Auszug)
Projekt kann angelegt und gelesen werden; projectKey wird validiert.
Sprachen können angelegt, gelistet, aktualisiert, soft-gelöscht, restored und gepurged werden (Constraints für Default/Fallback greifen).
Bundles können als JSON hochgeladen und heruntergeladen werden; ETag/If-None-Match liefert 304 wenn passend.
Runtime-Translations liefern Werte für full/keys/prefix; Default-/Fallback-Logik funktioniert; max. 500 Keys werden erzwungen.
Audit-Log wird bei Änderungen geschrieben und kann gefiltert abgerufen werden.
Docker Compose Start funktioniert (MariaDB + Service), Health Endpoint ist erreichbar.

12. MUSS / SOLL / KANN Übersicht
Bereich
MUSS
SOLL
KANN
Projekt
Create/Get/List
–
–
Sprachen
CRUD + Soft-Delete/Restore/Purge
Views (active/all/deleted)
–
Language Settings
Default/Fallback + Optimistic Lock
–
–
Bundles
Upload/Download JSON
Versioning/Rollback/Diff
Weitere Formate (YAML etc.)
Runtime Translations
full/keys/prefix + Default/Fallback
Caching
–
Audit
–
Audit Log + Filter
–
Security
–
API-Key Filter (minimal)
Spring Security / IAM (später)

13. Backlog / optionale Erweiterungen (Auszug)
Integration Spring Security + JWT/OAuth (Rollen/Rechte statt API-Key).
Unterstützung weiterer Bundle-Formate (YAML, i18next, ICU MessageFormat).
Mehrstufige Fallback-Ketten und Regional-Fallback (z. B. de-CH -> de-DE -> en-GB).
API für „Missing Keys“ Reporting/Export (QA für Übersetzungen).
Streaming/Compression für sehr große Bundles (ETag + gzip).
Aufräum-Jobs für nicht mehr referenzierte Bundle-Dateien (Storage Hygiene).

